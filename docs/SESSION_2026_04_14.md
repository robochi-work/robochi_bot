# Сессия 14.04.2026 — Изменения

## 1. Тексты блокировки (3 файла)
- **Постоянная**: "Вас заблоковано у сервісі robochi.work !\nДля розблокування зверніться до Адміністратора- @robochi_work_admin"
- **Временная**: "Увага! Вас обмежено у користуванні сервісом robochi.work !\nДля розблокування зверніться до Адміністратора- @robochi_work_admin"
- Файлы: `telegram/handlers/messages/commands.py`, `work/views/admin_panel.py`, `vacancy/services/call_formatter.py`
- `days_text` и `block =` убраны как unused (ruff F841)

## 2. Дубль уведомления при одобрении вакансии
- Убрано сообщение 3 (дубль) — метод `_add_employer_to_group` удалён из `vacancy/services/observers/approved_group_observer.py`
- Текст сообщения 2 обновлён: "Вашу вакансію схвалено та опубліковано для пошуку робітників..."
- `employer_invite_msg_id` больше не устанавливается (сообщение удалено)

## 3. Перекличка «Окончание работы» — тайминг
- Создана `get_final_call_vacancies(before_end=60)` — ловит вакансии за 1 час до end_time
- `final_call_check_task` использует новую функцию вместо `get_final_vacancies()`
- `get_final_vacancies()` оставлена для `close_vacancy_task` (нужна проверка after end_time)

## 4. Перекличка «Начало работы» — увеличен лимит напоминаний
- `_MAX_REMINDERS = 12` (было 6) → 60 минут ожидания вместо 30

## 5. Кнопка переклички в ЛК
- После прохождения 1-й переклички сразу показывается кнопка 2-й переклички
- В шаблоне `call.html`: заголовки "Початок роботи" / "Кінець роботи"
- Ссылка "Повернутися до першої переклички" в окне 2-й переклички (пока не пройдена)

## 6. Права заказчика в группе
- `can_restrict_members=False` в `set_default_owner_permissions()` — заказчик не может кикать/менять разрешения в Telegram
- Удаление рабочих — только через ЛК (страница members)
- Заказчик остаётся админом (для тега "Роботодавець"), фото/видео доступны через chat_permissions

## 7. Single-instance guard для WebApp
- `lifecycle.js` v6: BroadcastChannel закрывает предыдущее окно WebApp при открытии нового
- Reload с retry (500ms, 1000ms, 1500ms) и fallback tap-to-reload
- Порог freeze увеличен до 3000ms

## 8. Fallback в check.html
- При пустом `initData` (открытие не через WebApp) — информативное сообщение с кнопкой "Перейти до бота"

## 9. Favicon
- Файлы: `favicon.ico`, `favicon-32x32.png`, `apple-touch-icon.png` в `telegram/static/`
- Подключены в `templates/base.html`

## 10. Форма создания вакансии — время
- `start_time` и `end_time` убраны из initial шаблона последней вакансии
- Время всегда пересчитывается от текущего момента (now+1h rounded to 15min)

## 11. Навигация админа после модерации
- Редирект после approve → `admin_dashboard` (было `vacancy:my_list?for_user=X`)
- Кнопка «Назад» в `admin_vacancy_card.html` → `admin_dashboard`
- Кнопка «Назад» в `vacancy_my_list.html` (admin view) → `admin_dashboard`
- Устранено зацикливание history.back() между страницами
